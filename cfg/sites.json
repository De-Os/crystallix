{
  "@": {
    "_comment": "Use absolute path (/./..) or Crystallix will use its folder as path. Dont use '/' at the end of path",
    "path": "pages/tests",
    "index": ["index.php", "index.html"],
    "errors": {
      "redirect": true,
      "_comment": "%site_path% tells Crystallix to use current site path",
      "404": "%site_path%/errors/404.html"
    }
  }
}
